# Changesets

Hello and welcome! This folder contains the release notes used to version and publish this package.

Add a Markdown file here for each user-facing change. The Changesets action turns pending entries into a version PR and publishes it after that PR is merged.
